import AdminApp from '../../admin/AdminApp';

export const metadata = { title: 'Panel Admin | Äura', robots: { index: false, follow: false } };

export default function AdminPage() {
  return <AdminApp />;
}
